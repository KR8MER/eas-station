# IPAWS (FEMA) Alert Poller Configuration
# This file is loaded by the ipaws-poller service
# Copy to /app-config/ipaws.env (or let the setup wizard create it)

# CAP Poller Mode (should be IPAWS for this service)
CAP_POLLER_MODE=IPAWS

# IPAWS Feed URLs (comma-separated)
# Production: https://apps.fema.gov/IPAWSOPEN_EAS_SERVICE/rest/public/recent/{timestamp}
# Staging/Test: https://tdl.apps.fema.gov/IPAWSOPEN_EAS_SERVICE/rest/public/recent/{timestamp}
IPAWS_CAP_FEED_URLS=https://apps.fema.gov/IPAWSOPEN_EAS_SERVICE/rest/public/recent/2024-01-01T00:00:00Z

# Alternatively, use CAP_ENDPOINTS (same as IPAWS_CAP_FEED_URLS)
# CAP_ENDPOINTS=https://apps.fema.gov/IPAWSOPEN_EAS_SERVICE/rest/public/recent/2024-01-01T00:00:00Z

# Lookback period for default IPAWS feed (hours)
# IPAWS_DEFAULT_LOOKBACK_HOURS=12

# Optional: SSL certificate verification
# Set to 1 to disable SSL verification (not recommended for production)
# SSL_VERIFY_DISABLE=0

# Optional: Custom CA bundle path
# CAP_POLLER_CA_BUNDLE=/path/to/ca-bundle.crt

# Optional: Enable debug logging for alert matching
# CAP_POLLER_DEBUG_RECORDS=1

# Optional: Enable radio captures when alerts are processed
# CAP_POLLER_ENABLE_RADIO=1
