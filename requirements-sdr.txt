# Requirements for SDR Hardware Service (venv-sdr)
# This venv uses --system-site-packages to access python3-soapysdr from apt
#
# The SDR service imports from app_core which has many dependencies.
# This list includes everything needed for those imports to work.
# We exclude gunicorn/gevent (web server only) and hardware display packages.

# Core Flask (needed for app_core imports)
Flask>=3.0.0
Flask-SQLAlchemy>=3.1.1
Flask-Caching>=2.3.0
Werkzeug>=3.0.0
Jinja2>=3.1.0
itsdangerous>=2.0.0

# Database
SQLAlchemy>=2.0.0
psycopg2-binary>=2.9.9
GeoAlchemy2>=0.15.0
greenlet>=3.0.0

# Redis
redis>=4.5.0
hiredis>=3.0.0

# System and utilities
psutil>=5.9.0
python-dotenv>=1.0.0
PyYAML>=6.0.0
pytz>=2024.1
python-dateutil>=2.8.0

# JSON parsing
orjson>=3.9.0

# HTTP client (for app_utils)
requests>=2.31.0
certifi>=2024.0.0

# XML parsing (for CAP alerts in app_core)
lxml>=6.1.3

# Signal processing
numpy>=2.5.2
scipy>=1.12.0
numba>=0.67.0,<0.68.0  # JIT compilation for real-time FM demodulation (optional but recommended). Matches requirements.txt's pin -- every numba<0.65 release caps numpy at <2.4, which conflicts with numpy>=2.5.2 above and breaks `pip install -r requirements-sdr.txt` outright (ResolutionImpossible). numba>=0.67.0 is the first release supporting numpy 2.5.x; already proven working in the main venv.

# Audio processing
pydub>=0.25.0
audioop-lts>=0.2.0
