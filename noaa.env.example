# NOAA Weather Alert Poller Configuration
# This file is loaded by the noaa-poller service
# Copy to /app-config/noaa.env (or let the setup wizard create it)

# CAP Poller Mode (should be NOAA for this service)
CAP_POLLER_MODE=NOAA

# NOAA poller automatically builds endpoints from zone codes
# No need to specify CAP_ENDPOINTS or IPAWS_CAP_FEED_URLS

# Optional: Enable debug logging for alert matching
# CAP_POLLER_DEBUG_RECORDS=1

# Optional: Enable radio captures when alerts are processed
# CAP_POLLER_ENABLE_RADIO=1
