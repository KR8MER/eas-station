# EAS Station Environment Configuration
#
# This file is intentionally empty and tracked in git to prevent Docker from
# creating it as a directory during Portainer deployments.
#
# IMPORTANT: Do not add secrets or sensitive data here manually.
#
# Configuration Methods:
#   1. Web Setup Wizard (Recommended): Navigate to http://localhost/setup
#      - The wizard will populate this file automatically
#      - Provides dropdowns, validation, and auto-derivation features
#
#   2. Manual Configuration: Copy values from .env.example
#      - Generate SECRET_KEY: python3 -c "import secrets; print(secrets.token_hex(32))"
#      - Edit this file with your values
#      - Restart containers after making changes
#
# After configuration, this file will contain your settings.
# The .gitignore is configured to prevent committing changes to this file.
